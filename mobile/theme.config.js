/** @type {const} */
const themeColors = {
  primary: { light: '#e8843c', dark: '#e8843c' },   // orange accent — matches web buttons/CTAs
  background: { light: '#a8d4b8', dark: '#1c2e24' }, // light forest green / deep forest green
  surface: { light: '#ffffff', dark: '#243d2e' },    // white cards / raised forest surface
  foreground: { light: '#2c2c2c', dark: '#a8d4b8' }, // near-black / light forest green
  muted: { light: '#7a6a58', dark: '#8a9e90' },      // warm bark-brown / muted sage
  border: { light: '#ddd5c4', dark: '#2d5a3d' },     // warm tan / forest green
  success: { light: '#2d5a3d', dark: '#4a8060' },    // forest green / forest-light
  warning: { light: '#F59E0B', dark: '#FBBF24' },
  error: { light: '#EF4444', dark: '#F87171' },
};

module.exports = { themeColors };
